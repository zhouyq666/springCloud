server:
  port:  6611
  servlet:
    context-path: /account


eureka:
  instance:
    hostname: account_management
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      defaultZone: http://localhost:8761/eureka/, http://localhost:8762/eureka/


spring:
  application:
    name: account_management
